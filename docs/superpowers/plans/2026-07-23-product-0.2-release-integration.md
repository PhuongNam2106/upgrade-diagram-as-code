# Product 0.2 Release Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ghép extension Marketplace và Windows installer thành release `product-v0.2.0` có version đồng bộ, artifact kiểm chứng được, Gateway image công khai và runbook phát hành/rollback rõ ràng.

**Architecture:** `prepare-release.mjs` là release orchestrator duy nhất. Nó kiểm tra version của toàn bộ workspace, đóng gói VSIX, Action bundle và Windows Server ZIP, sau đó tạo manifest cùng SHA-256. Workflow tag build GHCR image trước, rồi mới phát hành artifact; Marketplace vẫn được upload thủ công từ chính VSIX đã qua release gate.

**Tech Stack:** Node.js 24, npm workspaces, `adm-zip`, GitHub Actions, GHCR, `@vscode/vsce`, Docker Buildx, PowerShell.

## Global Constraints

- Release version mục tiêu là `0.2.0`; Git tag là `product-v0.2.0`.
- Extension Marketplace ID phải là `phuongnam.diagram-as-code-vscode`.
- Gateway image khóa theo tag `ghcr.io/phuongnam2106/diagram-as-code-gateway:product-v0.2.0`; không dùng riêng `latest` trong manifest installer.
- Windows ZIP phải self-contained về script/config nhưng không nhúng Docker image; installer pull image từ GHCR.
- `SHA256SUMS` phải chứa VSIX, Action bundle, Windows ZIP và các deployment artifact.
- Không đặt Marketplace PAT, API key, `GITHUB_TOKEN` hoặc secret thật vào source, ZIP, manifest hay log.
- Codex chỉ sửa và test local. Người dùng tự commit, push, tag, chỉnh GHCR visibility, tạo/kiểm tra GitHub Release và upload Marketplace.

**Prerequisite Plans:**

- `docs/superpowers/plans/2026-07-23-marketplace-extension.md`
- `docs/superpowers/plans/2026-07-23-windows-server-installer.md`

## File Structure

- `product/scripts/create-server-bundle.mjs`: tạo ZIP và checksum nội bộ của server package.
- `product/scripts/create-server-bundle.test.mjs`: kiểm nội dung ZIP, manifest và orchestration order.
- `product/scripts/validate-release-versions.mjs`: kiểm version graph của npm workspaces.
- `product/scripts/validate-release-versions.test.mjs`: unit tests cho version graph.
- `product/scripts/prepare-release.mjs`: orchestrator tạo toàn bộ release artifact, manifest và checksum ngoài.
- `product/scripts/docs-release-contract.test.mjs`: marker contract cho tài liệu bàn giao.
- `product/docs/releases/0.2.0.md`: release notes và release-candidate record.
- `.github/workflows/product-release.yml`: build GHCR image trước, sau đó tạo GitHub Release.
- `product/docs/release-guide.md`: runbook local/user Git/GHCR/Marketplace/post-release.

---

## Task 1: Tạo Windows Server ZIP bằng code có test

**Files:**

- Create: `product/scripts/create-server-bundle.mjs`
- Create: `product/scripts/create-server-bundle.test.mjs`
- Modify: `product/package.json`
- Modify: `product/package-lock.json`

**Interfaces:**

- Consumes: installer files từ Windows installer plan, product version, Gateway image tag và renderer lock.
- Produces: `createServerBundle(options) -> Promise<{ fileName, outputPath, manifest }>` và ZIP 7-file không chứa secret.

- [ ] **Step 1: Cài ZIP library dưới dạng dependency trực tiếp**

Run from `product`:

```powershell
npm install --save-dev adm-zip@0.6.0
```

Expected: `package.json` và `package-lock.json` ghi đúng dependency trực tiếp. Không dựa vào dependency bắc cầu của `vsce`.

- [ ] **Step 2: Viết test RED cho nội dung ZIP và manifest**

```javascript
import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { mkdtemp, readFile, rm } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import test from "node:test";

import AdmZip from "adm-zip";
import { createServerBundle } from "./create-server-bundle.mjs";

const productRoot = path.resolve(import.meta.dirname, "..");

test("creates a self-contained versioned Windows server ZIP", async () => {
  const releaseDirectory = await mkdtemp(path.join(os.tmpdir(), "diagram-server-release-"));
  try {
    const result = await createServerBundle({
      productRoot,
      releaseDirectory,
      version: "9.8.7",
      dockerImage: "ghcr.io/example/diagram-as-code-gateway:product-v9.8.7",
      rendererLock: { kroki: "0.31.1", mermaid: "0.31.1" },
    });

    assert.equal(result.fileName, "diagram-as-code-server-9.8.7.zip");
    const zip = new AdmZip(result.outputPath);
    assert.deepEqual(
      zip.getEntries().map((entry) => entry.entryName).sort(),
      [
        "DiagramServer.psm1",
        "LICENSE",
        "README.md",
        "SHA256SUMS",
        "diagram-server.ps1",
        "docker-compose.yml",
        "server-manifest.json",
      ],
    );

    const manifest = JSON.parse(zip.readAsText("server-manifest.json"));
    assert.equal(manifest.schemaVersion, 1);
    assert.equal(manifest.productVersion, "9.8.7");
    assert.equal(manifest.gatewayUrl, "http://localhost:9000");
    assert.equal(manifest.images.gateway, "ghcr.io/example/diagram-as-code-gateway:product-v9.8.7");
    assert.equal(manifest.images.kroki, "yuzutech/kroki:0.31.1");
    assert.equal(manifest.images.mermaid, "yuzutech/kroki-mermaid:0.31.1");
    for (const line of zip.readAsText("SHA256SUMS").trim().split("\n")) {
      const [expected, name] = line.split(/\s+/, 2);
      const content = zip.readFile(name);
      assert.ok(content, `missing checksummed entry: ${name}`);
      assert.equal(createHash("sha256").update(content).digest("hex"), expected);
    }
    assert.equal((await readFile(result.outputPath)).length > 0, true);
  } finally {
    await rm(releaseDirectory, { recursive: true, force: true });
  }
});
```

- [ ] **Step 3: Chạy test RED**

Run:

```powershell
node --test .\scripts\create-server-bundle.test.mjs
```

Expected: fail vì `create-server-bundle.mjs` chưa tồn tại.

- [ ] **Step 4: Implement bundle builder**

```javascript
import { createHash } from "node:crypto";
import { mkdir, readFile } from "node:fs/promises";
import path from "node:path";

import AdmZip from "adm-zip";

export async function createServerBundle({
  productRoot,
  releaseDirectory,
  version,
  dockerImage,
  rendererLock,
}) {
  if (!/^\d+\.\d+\.\d+$/.test(version)) throw new Error(`Invalid product version: ${version}`);
  if (!dockerImage.endsWith(`:product-v${version}`)) {
    throw new Error(`Gateway image must use product-v${version}`);
  }

  const manifest = {
    schemaVersion: 1,
    productVersion: version,
    composeProject: "diagram-as-code",
    gatewayUrl: "http://localhost:9000",
    readinessTimeoutSeconds: 120,
    images: {
      gateway: dockerImage,
      kroki: `yuzutech/kroki:${rendererLock.kroki}`,
      mermaid: `yuzutech/kroki-mermaid:${rendererLock.mermaid}`,
    },
  };

  const entries = [
    ["windows-installer/diagram-server.ps1", "diagram-server.ps1"],
    ["windows-installer/DiagramServer.psm1", "DiagramServer.psm1"],
    ["windows-installer/README.md", "README.md"],
    ["deploy/docker-compose.release.yml", "docker-compose.yml"],
  ];

  const contentEntries = new Map();
  for (const [source, destination] of entries) {
    contentEntries.set(destination, await readFile(path.join(productRoot, source)));
  }
  contentEntries.set("LICENSE", await readFile(path.resolve(productRoot, "..", "LICENSE")));
  contentEntries.set("server-manifest.json", Buffer.from(`${JSON.stringify(manifest, null, 2)}\n`, "utf8"));

  const checksumLines = [...contentEntries.entries()]
    .sort(([left], [right]) => left.localeCompare(right))
    .map(([name, content]) => `${createHash("sha256").update(content).digest("hex")}  ${name}`);
  contentEntries.set("SHA256SUMS", Buffer.from(`${checksumLines.join("\n")}\n`, "utf8"));

  const zip = new AdmZip();
  for (const [name, content] of contentEntries) zip.addFile(name, content);

  await mkdir(releaseDirectory, { recursive: true });
  const fileName = `diagram-as-code-server-${version}.zip`;
  const outputPath = path.join(releaseDirectory, fileName);
  zip.writeZip(outputPath);
  return { fileName, outputPath, manifest };
}
```

- [ ] **Step 5: Thêm test script và chạy GREEN**

Thêm vào `product/package.json`:

```json
"test:server-bundle": "node --test ./scripts/create-server-bundle.test.mjs"
```

Run:

```powershell
npm run test:server-bundle
```

Expected: 1 test pass và thư mục temp được dọn.

- [ ] **Step 6: Checkpoint Git thủ công**

Người dùng tự review và commit Task 1.

---

## Task 2: Tích hợp ZIP vào release orchestrator và workflow

**Files:**

- Modify: `product/scripts/prepare-release.mjs`
- Modify: `product/package.json`
- Modify: `.github/workflows/product-release.yml`
- Modify: `.github/workflows/product-ci.yml`

**Interfaces:**

- Consumes: `createServerBundle` từ Task 1 và artifact release hiện có.
- Produces: release metadata/checksum chứa Server ZIP; workflow build Gateway image trước khi tạo GitHub Release.

- [ ] **Step 1: Viết release orchestration contract RED**

Thêm test này vào `product/scripts/create-server-bundle.test.mjs`; nó buộc orchestrator tạo ZIP trước khi quét artifact để manifest/checksum tự động bao gồm ZIP:

```javascript
test("release orchestration creates the server ZIP before describing artifacts", async () => {
  const source = await readFile(path.join(productRoot, "scripts", "prepare-release.mjs"), "utf8");
  const importIndex = source.indexOf('import { createServerBundle } from "./create-server-bundle.mjs";');
  const bundleIndex = source.indexOf("await createServerBundle({");
  const artifactScanIndex = source.indexOf("const artifactNames = (await readdir(releaseDirectory)).sort();");

  assert.notEqual(importIndex, -1);
  assert.notEqual(bundleIndex, -1);
  assert.notEqual(artifactScanIndex, -1);
  assert.equal(bundleIndex < artifactScanIndex, true);
});
```

Task 5 sẽ chạy `release:prepare` thật và kiểm tra `manifest.json`, `SHA256SUMS` cùng `dockerImage` trong ZIP, sau khi VSIX/Action đã được build.

- [ ] **Step 2: Chạy RED**

Run:

```powershell
npm run test:server-bundle
```

Expected: fail tại `assert.notEqual(importIndex, -1)` vì release orchestrator chưa import bundle builder.

- [ ] **Step 3: Gọi bundle builder từ `prepare-release.mjs`**

Import:

```javascript
import { createServerBundle } from "./create-server-bundle.mjs";
```

Sau khi copy các artifact tĩnh và trước `readdir(releaseDirectory)`, gọi:

```javascript
await createServerBundle({
  productRoot,
  releaseDirectory,
  version,
  dockerImage,
  rendererLock,
});
```

Không hard-code `0.2.0` trong builder; version luôn lấy từ workspace manifests.

- [ ] **Step 4: Gộp test mới vào release gate**

Thêm script:

```json
"test:release-tools": "npm run test:installer-contract && npm run test:server-bundle"
```

Đổi `release:prepare` thành:

```json
"release:prepare": "npm audit && npm run typecheck && npm test && npm run test:release-tools && npm run build && npm --workspace=diagram-as-code-vscode run package && npm run release:bundle"
```

- [ ] **Step 5: Upload ZIP trong GitHub Release**

Trong lệnh `gh release create`, thêm:

```text
product/release/${GITHUB_REF_NAME}/diagram-as-code-server-*.zip
product/release/${GITHUB_REF_NAME}/RELEASE_NOTES.md
```

Để tránh release xuất hiện trước image, thêm vào job `artifacts`:

```yaml
needs: gateway-image
```

Job `gateway-image` không phụ thuộc `artifacts`.

- [ ] **Step 6: Chạy kiểm tra workflow tĩnh và test GREEN**

Run from `product`:

```powershell
npm run test:release-tools
npx --yes prettier@3.6.2 --check ..\.github\workflows\product-release.yml ..\.github\workflows\product-ci.yml
```

Expected: pass. Không trigger workflow GitHub.

- [ ] **Step 7: Checkpoint Git thủ công**

Người dùng tự review và commit Task 2.

---

## Task 3: Đồng bộ toàn bộ product lên `0.2.0`

**Files:**

- Modify: `product/package.json`
- Modify: `product/package-lock.json`
- Modify: `product/packages/contracts/package.json`
- Modify: `product/packages/diagram-config/package.json`
- Modify: `product/gateway/package.json`
- Modify: `product/vscode-extension/package.json`
- Modify: `product/github-action/package.json`
- Create: `product/scripts/validate-release-versions.mjs`
- Create: `product/scripts/validate-release-versions.test.mjs`
- Create: `product/docs/releases/0.2.0.md`
- Modify: `product/vscode-extension/CHANGELOG.md`

**Interfaces:**

- Consumes: workspace manifest list trong `prepare-release.mjs` và version mục tiêu `0.2.0`.
- Produces: version graph đồng nhất, internal dependency contract và release notes `0.2.0`.

- [ ] **Step 1: Viết version graph test RED**

Tạo `product/scripts/validate-release-versions.test.mjs`:

```javascript
import assert from "node:assert/strict";
import test from "node:test";

import { validateReleaseVersions } from "./validate-release-versions.mjs";

test("returns the shared version when workspace and internal dependencies match", () => {
  const manifests = [
    { name: "root", version: "0.2.0" },
    { name: "contracts", version: "0.2.0" },
    { name: "gateway", version: "0.2.0", dependencies: { "@diagram-as-code/contracts": "0.2.0" } },
  ];
  assert.equal(validateReleaseVersions(manifests), "0.2.0");
});

test("rejects a stale internal dependency", () => {
  const manifests = [
    { name: "root", version: "0.2.0" },
    { name: "extension", version: "0.2.0", dependencies: { "@diagram-as-code/contracts": "0.1.0" } },
  ];
  assert.throws(
    () => validateReleaseVersions(manifests),
    /extension depends on @diagram-as-code\/contracts@0\.1\.0; expected 0\.2\.0/,
  );
});
```

- [ ] **Step 2: Chạy test RED**

Run:

```powershell
node --test .\scripts\validate-release-versions.test.mjs
```

Expected: fail với `ERR_MODULE_NOT_FOUND` vì helper chưa tồn tại.

- [ ] **Step 3: Implement version graph validator**

Tạo `product/scripts/validate-release-versions.mjs`:

```javascript
export function validateReleaseVersions(manifests) {
  if (manifests.length === 0) throw new Error("At least one product manifest is required");
  const versions = new Set(manifests.map((manifest) => manifest.version));
  if (versions.size !== 1) {
    throw new Error(`Product package versions must match: ${[...versions].join(", ")}`);
  }

  const [version] = versions;
  const internalPackageNames = new Set([
    "@diagram-as-code/contracts",
    "@diagram-as-code/diagram-config",
  ]);
  for (const manifest of manifests) {
    for (const [name, dependencyVersion] of Object.entries(manifest.dependencies ?? {})) {
      if (internalPackageNames.has(name) && dependencyVersion !== version) {
        throw new Error(`${manifest.name} depends on ${name}@${dependencyVersion}; expected ${version}`);
      }
    }
  }
  return version;
}
```

Trong `prepare-release.mjs`, import helper và thay block `versions` hiện tại bằng:

```javascript
import { validateReleaseVersions } from "./validate-release-versions.mjs";

const version = validateReleaseVersions(packages);
```

Thêm script:

```json
"test:release-version": "node --test ./scripts/validate-release-versions.test.mjs",
"test:release-tools": "npm run test:installer-contract && npm run test:server-bundle && npm run test:release-version"
```

- [ ] **Step 4: Chạy version graph test GREEN**

Run:

```powershell
npm run test:release-version
npm run release:bundle
```

Expected: tests pass và release bundle hiện tại vẫn chuẩn bị được ở version đang đồng bộ.

- [ ] **Step 5: Bump workspace versions không tạo Git tag**

Run:

```powershell
npm version 0.2.0 --workspaces --include-workspace-root --no-git-tag-version
```

Sau lệnh, sửa các dependency nội bộ exact-version thành `0.2.0` tại:

- `product/gateway/package.json`
- `product/vscode-extension/package.json`
- `product/github-action/package.json`

Run:

```powershell
npm install --package-lock-only
```

Expected: mọi workspace package và lockfile cùng version `0.2.0`, không có Git tag mới.

- [ ] **Step 6: Viết release notes `0.2.0`**

Release notes tiếng Anh gồm:

- Marketplace-ready extension ID `phuongnam.diagram-as-code-vscode`.
- Windows Server ZIP và các command hỗ trợ.
- Loopback-only Gateway mặc định.
- Local API key generation, idempotent install, key rotation và update rollback.
- Pinned renderer versions Kroki/Mermaid `0.31.1`.
- Upgrade từ `0.1.0`: extension settings giữ nguyên; server cũ cần chạy installer mới; API key có thể được tái sử dụng nếu đưa `.env` vào state directory theo runbook.
- Known limitation: Windows + Docker Desktop only; GHCR image phải public; GitHub Action local-Gateway cần self-hosted runner cùng máy/mạng.

- [ ] **Step 7: Đồng bộ extension changelog**

Mục `0.2.0` trong `product/vscode-extension/CHANGELOG.md` phải mô tả Marketplace metadata/onboarding/package hardening và không tuyên bố server được nhúng trực tiếp vào extension.

- [ ] **Step 8: Verify version graph**

Run:

```powershell
npm ci
npm run typecheck
npm test
npm run build
npm run release:bundle
```

Expected:

- không có version mismatch;
- `product/release/product-v0.2.0/diagram-as-code-server-0.2.0.zip` tồn tại;
- manifest dùng Gateway tag `product-v0.2.0`.

- [ ] **Step 9: Checkpoint Git thủ công**

Người dùng tự review và commit Task 3. Không tạo tag ở bước này.

---

## Task 4: Cập nhật runbook cài đặt, phát hành và rollback

**Files:**

- Modify: `product/docs/release-guide.md`
- Modify: `product/README.md`
- Modify: `Tech_Doc.md`
- Modify: `specs/001-diagram-as-code-platform/spec.md`

**Interfaces:**

- Consumes: hành vi thực tế của extension, Windows installer và release pipeline.
- Produces: tài liệu bàn giao nhất quán cùng automated marker test cho Windows/Marketplace delivery.

- [ ] **Step 1: Thêm acceptance test tài liệu RED**

Tạo test Node trong `product/scripts/docs-release-contract.test.mjs` kiểm tra các tài liệu có những marker hành vi, không khóa cả câu văn:

```javascript
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import path from "node:path";
import test from "node:test";

const root = path.resolve(import.meta.dirname, "..", "..");

test("release documentation covers Windows and Marketplace delivery", async () => {
  const releaseGuide = await readFile(path.join(root, "product/docs/release-guide.md"), "utf8");
  const productReadme = await readFile(path.join(root, "product/README.md"), "utf8");
  const techDoc = await readFile(path.join(root, "Tech_Doc.md"), "utf8");

  for (const marker of ["diagram-as-code-server-0.2.0.zip", "product-v0.2.0", "phuongnam", "SHA256SUMS", "GHCR"]) {
    assert.match(releaseGuide, new RegExp(marker, "i"));
  }
  assert.match(productReadme, /Windows Server/i);
  assert.match(productReadme, /Visual Studio Marketplace/i);
  assert.match(techDoc, /Docker Desktop/i);
  assert.match(techDoc, /GHCR/i);
});
```

Thêm script:

```json
"test:release-docs": "node --test ./scripts/docs-release-contract.test.mjs"
```

- [ ] **Step 2: Chạy RED**

Run:

```powershell
npm run test:release-docs
```

Expected: fail vì tài liệu hiện chưa cover đầy đủ artifact `0.2.0` và Marketplace.

- [ ] **Step 3: Cập nhật release guide bằng tiếng Việt**

Luồng phải phân biệt rõ:

1. Codex/local preparation: `npm ci`, `npm run release:prepare`, kiểm SHA-256, test VSIX/ZIP.
2. User Git checkpoint: review, commit, push.
3. User tag checkpoint: tạo/push `product-v0.2.0` để workflow build GHCR rồi tạo GitHub Release.
4. User GHCR checkpoint: đặt package Gateway thành Public và thử `docker pull` không login.
5. User Marketplace checkpoint: dùng VSIX từ release, đăng nhập publisher `phuongnam`, upload/publish thủ công.
6. Post-release validation: tải artifact như người dùng mới, xác minh SHA-256, cài server, cài extension, preview/export, chạy Diagram Action bằng self-hosted runner.

Thêm migration `0.1.0` chính xác: lưu giá trị `DIAGRAM_API_KEYS` hiện tại, dừng Compose stack cũ để giải phóng port 9000, đặt key vào `%LOCALAPPDATA%\DiagramAsCode\server\.env`, rồi chạy installer `0.2.0`. Installer phải giữ key đó; extension Marketplace vẫn yêu cầu nhập lại một lần vì extension ID đã đổi.

- [ ] **Step 4: Cập nhật tài liệu sản phẩm và spec**

`product/README.md` phải đưa hai đường cài đặt lên gần đầu:

- Extension: Visual Studio Marketplace `phuongnam.diagram-as-code-vscode`.
- Server Windows: GitHub Release ZIP + Docker Desktop.

`Tech_Doc.md` bổ sung đúng mô hình: installer không chứa server binary; nó dùng Docker Desktop để pull Gateway/Kroki image và chạy Compose local.

Spec bổ sung acceptance criteria phát hành nhưng không biến implementation detail thành user story mới.

- [ ] **Step 5: Chạy docs test GREEN**

Run:

```powershell
npm run test:release-docs
```

Expected: pass và các file đọc đúng UTF-8 tiếng Việt.

- [ ] **Step 6: Checkpoint Git thủ công**

Người dùng tự review và commit Task 4.

---

## Task 5: Chạy release candidate gate cục bộ

**Files:**

- Modify if needed: `product/docs/releases/0.2.0.md`
- Inspect only: `product/release/product-v0.2.0/*`

**Interfaces:**

- Consumes: toàn bộ code/artifact contract từ Task 1-4.
- Produces: release candidate đã qua clean install, checksum inspection, VSIX smoke và installer tests.

- [ ] **Step 1: Chạy full release gate từ clean dependencies**

Run:

```powershell
Set-Location D:\upgrade-diagram-as-code\product
npm ci
npm run release:prepare
```

Expected: audit, typecheck, tests, build, VSIX package và release bundle đều pass.

- [ ] **Step 2: Xác minh artifact và checksum**

Run:

```powershell
$release = "D:\upgrade-diagram-as-code\product\release\product-v0.2.0"
Get-ChildItem -LiteralPath $release | Select-Object Name, Length
Get-FileHash -Algorithm SHA256 "$release\diagram-as-code-vscode-0.2.0.vsix"
Get-FileHash -Algorithm SHA256 "$release\diagram-as-code-server-0.2.0.zip"
Get-Content "$release\SHA256SUMS"
```

Expected: hash PowerShell trùng hai dòng tương ứng trong `SHA256SUMS`; cả hai artifact có kích thước lớn hơn 0.

- [ ] **Step 3: Inspect VSIX và ZIP**

Run:

```powershell
tar -tf .\release\product-v0.2.0\diagram-as-code-vscode-0.2.0.vsix
Expand-Archive -LiteralPath .\release\product-v0.2.0\diagram-as-code-server-0.2.0.zip -DestinationPath "$env:TEMP\diagram-server-0.2.0" -Force
Get-ChildItem "$env:TEMP\diagram-server-0.2.0"
```

Expected:

- VSIX chỉ chứa allowlist đã định nghĩa trong Marketplace plan.
- Server ZIP có đúng 7 file contract, trong đó `SHA256SUMS` nội bộ kiểm tra sáu file còn lại.
- Không có `.env`, key, PAT, `.git`, test fixture hoặc `node_modules`.

- [ ] **Step 4: Chạy extension smoke test với profile sạch**

Theo Marketplace plan: cài VSIX vào VS Code profile tạm, cấu hình `http://localhost:9000`, set API key qua command, thử preview và export.

- [ ] **Step 5: Chạy Windows installer unit/static test lần cuối**

Run:

```powershell
npm run test:release-tools
powershell -NoLogo -NoProfile -Command "Invoke-Pester .\windows-installer\test -CI -Output Detailed"
pwsh -NoLogo -NoProfile -Command "Invoke-Pester .\windows-installer\test -CI -Output Detailed"
```

Expected: tất cả pass.

- [ ] **Step 6: Ghi kết quả release candidate**

Trong phần nội bộ cuối `product/docs/releases/0.2.0.md`, ghi ngày test và các gate pass, nhưng không ghi secret, username máy hay đường dẫn cá nhân.

---

## Task 6: Phát hành thủ công và kiểm tra như người dùng mới

**Files:**

- No source changes expected.
- User-managed external state: Git repository, GitHub Actions, GHCR, GitHub Release, Visual Studio Marketplace.

**Interfaces:**

- Consumes: release candidate pass và các artifact/checksum từ Task 5.
- Produces: GitHub Release công khai, GHCR image public, Marketplace extension `0.2.0` và biên bản bàn giao.

- [ ] **Step 1: User review và Git publish checkpoint**

Người dùng tự kiểm tra `git diff`, commit, push `main`, chờ required checks pass. Codex không chạy các lệnh này.

- [ ] **Step 2: User tạo release tag**

Sau khi CI main pass, người dùng tự tạo và push annotated tag `product-v0.2.0`. Tag kích hoạt workflow `Product Release`; Codex không tạo/push tag hoặc trigger workflow.

- [ ] **Step 3: User xác nhận GitHub Release và GHCR**

Checklist:

- Job `gateway-image` pass trước job `artifacts`.
- GitHub Release có VSIX, Action CJS, Server ZIP, Compose, env example, config example, manifest và `SHA256SUMS`.
- GHCR có tag `product-v0.2.0` cho `linux/amd64` và `linux/arm64`.
- Package Gateway được đặt `Public` thủ công.
- Từ terminal chưa login GHCR, `docker pull ghcr.io/phuongnam2106/diagram-as-code-gateway:product-v0.2.0` thành công.

- [ ] **Step 4: User phát hành Marketplace thủ công**

Dùng chính file VSIX tải từ GitHub Release, kiểm checksum rồi đăng nhập publisher `phuongnam`. Chạy theo runbook Marketplace; không tạo PAT trong repository và không dán PAT vào log/chat.

Acceptance:

- Listing hiển thị publisher `phuongnam` và version `0.2.0`.
- ID cài đặt là `phuongnam.diagram-as-code-vscode`.
- Cài trực tiếp từ Marketplace trên VS Code profile sạch thành công.

- [ ] **Step 5: User test Windows ZIP từ GitHub Release**

Trên máy Windows hiện tại:

1. Tải ZIP từ release, không dùng bản trong `product/release`.
2. So SHA-256 với release.
3. Giải nén vào thư mục mới.
4. Chạy `install`, `status`, render HTTP thật.
5. Cài extension từ Marketplace, set URL/key, preview và export `.mmd`.
6. Chạy GitHub Action qua self-hosted runner cùng máy và xác nhận Diagram check pass.

- [ ] **Step 6: Thực hành rollback trước khi chốt release**

- Extension: cài lại VSIX `0.1.0` bằng `code --install-extension "D:\Downloads\diagram-as-code-vscode-0.1.0.vsix" --force`.
- Server: dùng ZIP version trước để chạy `update`; nếu format manifest không tương thích thì restore backup theo runbook.
- GitHub Action: consumer đổi `uses` về tag ổn định trước.
- Không di chuyển hoặc ghi đè tag `product-v0.2.0`; mọi sửa lỗi sau phát hành dùng `0.2.1`.

- [ ] **Step 7: Chốt biên bản bàn giao**

Ghi lại URL Marketplace, URL GitHub Release, GHCR image tag, checksum artifact, ngày smoke test và limitations. Không ghi API key hoặc token.

---

## Completion Criteria

- Tất cả package và internal dependency cùng version `0.2.0`.
- Release gate local pass từ `npm ci` sạch.
- GitHub Release có Server ZIP và checksum hợp lệ; image được build trước artifact release.
- GHCR Gateway image pull được công khai mà không cần login.
- Marketplace listing `phuongnam.diagram-as-code-vscode@0.2.0` cài được trên profile sạch.
- ZIP tải từ release cài được Gateway local, extension preview/export được, Action self-hosted gọi được Gateway.
- Runbook install/update/rotate/uninstall/rollback và tài liệu người dùng đã đồng bộ.
- Mọi thao tác Git/GitHub/Marketplace do người dùng chủ động thực hiện.
